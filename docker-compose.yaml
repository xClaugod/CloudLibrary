services:
  backend:
    image: xclaugod/backend:latest
    ports:
      - "8800:8800"
    networks:
      - mynetwork

  client:
    image: xclaugod/client:latest
    ports:
      - "3000:80"
    networks:
      - mynetwork
    depends_on:
      - backend
networks:
  mynetwork:
    driver: bridge